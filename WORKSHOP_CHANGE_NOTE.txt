v1.10.4 fixes the startup reconciliation crash and accepts large reconnect catch-ups when the server independently confirms the kill counter. Backups remain automatic, and totals or season standings are never reduced.
